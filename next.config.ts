import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  /* config options here */
}

// module.exports = {
//   allowedDevOrigins: ['letting-recall-mailto-firm.trycloudflare.com'],
// }

export default nextConfig
